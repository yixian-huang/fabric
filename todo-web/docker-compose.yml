version: '3'
services:
  todo-project-admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-project-admin
    ports:
      - "18080:80"
    restart: unless-stopped
    # 如果需要环境变量，可以在这里添加
    # environment:
    #   - NODE_ENV=production
    # 如果需要挂载卷，可以在这里添加
    # volumes:
      # - ./nginx.conf:/etc/nginx/conf.d/default.conf 